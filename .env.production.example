# ========================================
# PRODUCTION DATABASE CONFIGURATION
# ========================================
# Copy this file to .env and fill in your production database credentials

DB_HOST=your-production-db-host.com
DB_USER=your-production-db-user
DB_PASSWORD=your-production-db-password
DB_NAME=ytextractordb_production

# ========================================
# CHECKPOINT CONFIGURATION
# ========================================
# For Docker deployment, use the container path
CHECKPOINT_DIR=/data/checkpoints

# ========================================
# DEPLOYMENT NOTES
# ========================================
# 1. Copy this file: cp .env.production.example .env
# 2. Edit .env and fill in your actual production database credentials
# 3. NEVER commit the .env file to git
# 4. Run: docker-compose up -d
# 5. Check logs: docker-compose logs -f
