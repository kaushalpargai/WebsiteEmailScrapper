# ========================================
# DATABASE CONFIGURATION
# ========================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=ytextractordb_local

# ========================================
# CHECKPOINT CONFIGURATION (Server Portability)
# ========================================
# Optional: Specify custom checkpoint directory
# If not set, defaults to: ~/.youtube-scraper-data
# For server deployment, use absolute paths or network drives
#
# Linux/Unix Example:
#   CHECKPOINT_DIR=/var/lib/youtube-scraper/checkpoints
#
# Windows Example:
#   CHECKPOINT_DIR=C:\Data\youtube-scraper\checkpoints
#
# Network Drive Example (for migration between servers):
#   CHECKPOINT_DIR=/mnt/shared-storage/youtube-scraper-checkpoints
#
# Docker Example (when running in container):
#   CHECKPOINT_DIR=/data/checkpoints
#
# Leave commented out to use default location
CHECKPOINT_DIR=/var/lib/youtube-scraper/checkpoints
